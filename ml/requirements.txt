# Python ML dependencies
# Training uses PyTorch (GPU via CUDA) — already installed: torch 2.5.1+cu121
# Inference (Flask API) uses the saved .pt model via PyTorch

# Data & utils
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
matplotlib==3.7.2
joblib==1.3.1
tqdm==4.65.0

# Flask API server
flask==3.0.0
flask-cors==4.0.0

# PyTorch (GPU) — install separately with CUDA support:
# pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# TensorFlow (CPU fallback, already installed):
# tensorflow==2.13.1
#
# NOTE: tensorflow is NOT used for training or inference anymore.
# PyTorch handles all ML operations on CUDA GPU.
